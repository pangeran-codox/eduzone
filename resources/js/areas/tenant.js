import '../bootstrap';
import '../alpine';

// Taruh JS spesifik area tenant di sini (mis. logic dashboard per role, filter tabel,
// notifikasi real-time via Reverb, dsb). Semua role tenant (kepsek, guru, siswa, dst)
// berbagi entry ini karena sama-sama pakai layout & komponen yang mirip.
